{% if section.blocks.size > 0 %}
  <section class="main-cta">
    <div class="container">
      <div class="main-cta__inner-wrapper">
        {% assign grid_count = section.settings.grid_items | default: 3 %}
        {% if grid_count > 4 %}
          {% assign grid_count = 4 %}
        {% endif %}
        <div
          class="
            main-cta__list
            {% if section.settings.display_button != blank %}
              with-button
            {% endif %}
          "
          style="grid-template-columns: repeat({{ grid_count }}, 1fr);"
        >
          {% for block in section.blocks %}
            <div class="main-cta__item" {{ block.shopify_attributes }}>
              {% if block.settings.image != blank %}
                <div class="main-cta__item--image hover-zoom-image">
                  <img
                    src="{{ block.settings.image | image_url: width: 800 }}"
                    alt="{{ block.settings.image.alt | default: block.settings.title }}"
                    width="{{ block.settings.image.width | default: 800 }}"
                    height="{{ block.settings.image.height | default: 600 }}"
                    loading="lazy"
                    class="responsive-image"
                  >
                </div>
              {% endif %}
              <div class="main-cta__item--content">
                {% if block.settings.title != blank %}
                  <h3 class="main-cta__item--title site-title__md">{{ block.settings.title }}</h3>
                {% endif %}
                {% if block.settings.description != blank %}
                  <div class="main-cta__item--description general-content-container">
                    {{ block.settings.description }}
                  </div>
                {% endif %}
              </div>
              {% if section.settings.display_button
                and block.settings.button_label != blank
                and block.settings.button_link != blank
              %}
                <a href="{{ block.settings.button_link }}" class="btn main-cta__item--button">
                  {{ block.settings.button_label }}
                </a>
              {% endif %}
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
  </section>
{% endif %}

{% schema %}
{
  "name": "Main CTA",
  "settings": [
    {
      "type": "number",
      "id": "grid_items",
      "label": "Grid Items",
      "default": 3,
      "info": "Number of items per row (max: 4)"
    },
    {
      "type": "checkbox",
      "id": "display_button",
      "label": "Display Button",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "cta_item",
      "name": "CTA Item",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button Label"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Main CTA",
      "category": "DOJO",
      "settings": {
        "grid_items": 3,
        "display_button": false
      },
      "blocks": [
        {
          "type": "cta_item",
          "settings": {
            "title": "Designed With Intention",
            "description": "<p>Inspired by Japanese principles of restraint, balance, and doing less — but better.</p>",
            "image": "shopify://shop_images/7.jpg",
            "button_label": "",
            "button_link": ""
          }
        },
        {
          "type": "cta_item",
          "settings": {
            "title": "For the Long Game",
            "description": "<p>Created for people who are still moving, still training, and still showing up — without shortcuts.</p>",
            "image": "shopify://shop_images/6.jpg",
            "button_label": "",
            "button_link": ""
          }
        },
        {
          "type": "cta_item",
          "settings": {
            "title": "Grounded Where We’re From",
            "description": "<p>Shaped by Filipino warmth, openness, and connection. Manufactured in the Philippines, where daily movement and shared rituals are part of life.</p>",
            "image": "shopify://shop_images/9.jpg",
            "button_label": "",
            "button_link": ""
          }
        }
      ]
    }
  ]
}
{% endschema %}
